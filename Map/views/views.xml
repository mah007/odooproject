<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="map.list">
      <field name="name">Map list</field>
      <field name="model">map.map</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="map.action_window">
      <field name="name">Map window</field>
      <field name="res_model">map.map</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="map.action_server">
      <field name="name">Map server</field>
      <field name="model_id" ref="model_map_map"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Map" id="map.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="map.menu_1" parent="map.menu_root"/>
    <menuitem name="Menu 2" id="map.menu_2" parent="map.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="map.menu_1_list" parent="map.menu_1"
              action="map.action_window"/>
    <menuitem name="Server to list" id="map" parent="map.menu_2"
              action="map.action_server"/>
    -->
  </data>
</odoo>