<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mapps.list">
      <field name="name">Mapps list</field>
      <field name="model">mapps.mapps</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mapps.action_window">
      <field name="name">Mapps window</field>
      <field name="res_model">mapps.mapps</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mapps.action_server">
      <field name="name">Mapps server</field>
      <field name="model_id" ref="model_mapps_mapps"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Mapps" id="mapps.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mapps.menu_1" parent="mapps.menu_root"/>
    <menuitem name="Menu 2" id="mapps.menu_2" parent="mapps.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mapps.menu_1_list" parent="mapps.menu_1"
              action="mapps.action_window"/>
    <menuitem name="Server to list" id="mapps" parent="mapps.menu_2"
              action="mapps.action_server"/>
    -->

  </data>
</odoo>